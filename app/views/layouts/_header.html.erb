<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo03" aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="navbar-brand" href="#">ProjektBlogi</a>

  <div class="collapse navbar-collapse" id="navbarTogglerDemo03">
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <%= link_to :controller => 'blogs', :action=>'index' do %>
        <li class="nav-link active" style="text-decoration:none">Blogi</li>
      <%end %>
      <%= link_to :controller => 'kinds', :action=>'index' do %>
        <li class="nav-link" style="text-decoration:none">Rodzaje</li>
      <%end %>
      <%= link_to :controller => 'tags', :action=>'index' do %>
        <li class="nav-link" style="text-decoration:none">Tagi</li>
      <%end %>
    </ul>

    <% if (params[:controller] != 'sessions')%>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Witaj <%= current_user.username %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <a class="dropdown-item" href="#">Edytuj profil</a>
            <% if current_user.is_admin %>
              <%= link_to "Użytkownicy",users_path, {:class=>"dropdown-item"}%>
            <% end %>

            <% if current_user.blogs.count != 0 %>
              <hr>
            <% end %>

            <% for b in current_user.blogs %>
              <%= link_to b.name, blog_posts_path(b.id), {:class=>"dropdown-item"}%>
            <%end %>

          </div>
        </li>
          <%= link_to :controller => 'sessions', :action=>'destroy' do %>
            <li class="nav-link" style="text-decoration:none">Wyloguj</li>
          <%end %>
        <% else %>
          <%= link_to :controller => 'sessions', :action=>'new' do %>
            <li class="nav-link" style="text-decoration:none">Zaloguj</li>
          <%end %>
        <%end%>
      </ul>
    <%end %>

  </div>
</nav>